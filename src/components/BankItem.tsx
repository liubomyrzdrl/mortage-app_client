import React from 'react';
import { BankType } from '../types';
import { Box } from '@material-ui/core'

const BankItem: React.FC<BankType> = () => {
    return (
        <div>
            
        </div>
    );
};

export default BankItem;